package org.spsu.accounting.data.mapper

import org.spsu.accounting.data.domain.UserDO

public class UserDOMapper extends BaseMapper<UserDO>{
/*
	@Override
	protected UserDO map(Map data) {

		UserDO user = new UserDO()

        if (data.id)
            user.id = getValue(data, "id")
        if (data.username)
            user.username = getValue(data, "username")
        if (data.password)
            user.password = getValue(data, "password")
        if (data.first_name)
            user.firstName = getValue(data, "first_name")
        if (data.last_name)
            user.lastName = getValue(data, "last_name")
        if (data.active)
            user.active = getValue(data, "active")
        if (data.locked)
            user.locked = getValue(data, "locked")
        if (data.password_reset)
            user.passwordReset = getDateValue(data, "password_reset")
        if (data.email)
            user.email = getValue(data, "email")
        if (data.login_attempts)
            user.loginAttempts = getValue(data, "login_attempts")

		return user
	}
*/
}
