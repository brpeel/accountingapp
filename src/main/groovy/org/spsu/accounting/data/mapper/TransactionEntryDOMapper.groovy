package org.spsu.accounting.data.mapper

import org.spsu.accounting.data.domain.TransactionEntryDO

public class TransactionEntryDOMapper extends BaseMapper<TransactionEntryDO>{
/*
	@Override
	protected TransactionEntryDO map(Map data) {

		TransactionEntryDO transactionEntry = new TransactionEntryDO()

        if (data.id)
            transactionEntry.id = getValue(data, "id")
        if (data.transaction_id)
            transactionEntry.transactionId = getValue(data, "transaction_id")
        if (data.account_id)
            transactionEntry.accountId = getValue(data, "account_id")
        if (data.amount)
            transactionEntry.amount = getValue(data, "amount")

		return transactionEntry
	}
*/
}
